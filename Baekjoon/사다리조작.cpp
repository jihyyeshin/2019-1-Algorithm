#include <iostream>
#include <string>
#include <queue>
using namespace std;
//
//void change(string* a, int x, int y) {
//	string buf = a[x];
//	a[x] = a[y];
//	a[y] = buf;
//}
string* sortString(int n, string* a) {
	string *b=new string[n];
	for (int i = 0; i < n; i++) {
		if () {
			
		
		}
	}
	return b;
}

int main() {
	string buf;
	string input[270];
	queue <string>sd[10];// N은 10개 까지 가능
	int n, m, h;
	cin >> n >> m >> h;
	for (int i = 0; i < m; i++) {
		for (int j = 0; j < 2; j++) {
			cin >> buf;
			input[i].append(buf);
		}
	}

	//sortString(m, input);


}